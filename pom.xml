<?xml version="1.0" encoding="UTF-8"?>
<!-- Originally generated by Claude Code on 2026-06-25 -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.15</version>
        <relativePath/>
    </parent>

    <groupId>com.example.spring-patch-enum-bug-report</groupId>
    <artifactId>root</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>root</name>
    <description>
        Bug report: Spring Data REST JSON Patch `test` operation fails for enum fields.
        JsonPatchPatchConverter.valueFromJsonNode returns a raw String for textual JSON nodes,
        but SpelPath.getValue returns the actual enum constant. ObjectUtils.nullSafeEquals(String, Enum)
        is always false, so the test operation always fails when the field is an enum.
        Add a module per Spring Boot version to test against multiple versions.
    </description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <modules>
        <module>common</module>
        <module>spring-boot-30</module>
        <module>spring-boot-31</module>
        <module>spring-boot-32</module>
        <module>spring-boot-33</module>
        <module>spring-boot-34</module>
        <module>spring-boot-35</module>
        <module>spring-boot-40</module>
        <module>spring-boot-41</module>
    </modules>
</project>
